apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: posts-backend-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: posts-backend
                port:
                  number: 80
